<nav class="navbar navbar-expand-lg bg-secondary-background">
	<div class="container-fluid">

		<a class="navbar-brand" href="{{ path('app_home') }}">Vocabulary Learning</a>

		<button class="navbar-toggler" type="button" data-bs-toggle="collapse" data-bs-target="#navbarSupportedContent" aria-controls="navbarSupportedContent" aria-expanded="false" aria-label="Toggle navigation">
			<span class="navbar-toggler-icon"></span>
		</button>
		<div class="collapse navbar-collapse" id="navbarSupportedContent">
			<ul class="navbar-nav me-auto mb-2 mb-lg-0">
				{% if app.user %}
					<li class="nav-item">
						<a class="nav-link" href="{{ path('word_list_index') }}">My lists</a>
					</li>
					<li class="nav-item">
						<a class="nav-link" href="{{ path('language_index') }}">Languages</a>
					</li>
				{% endif %}

                {% if app.user and 'ROLE_ADMIN' in app.user.getRoles %}
                    <li class="nav-item">
					    <a class="nav-link" href="{{ path('admin') }}"><span class="text-danger">EasyAdmin</span></a>
				    </li>
                {% endif %}
			</ul>

            {% if app.user %}
                <div class="d-flex">
                    <a href="{{ path('app_profile') }}" class="btn btn-outline-success me-2 bg-secondary">Profile</a>
                    <a href="{{ path('app_logout') }}" class="btn btn-outline-success me-2 bg-primary">Logout</a>
                </div>
            {% else %}
                <div class="d-flex">
                    <a href="{{ path('app_login') }}" class="btn btn-outline-success me-2 bg-primary">Login</a>
                    <a href="{{ path('app_register') }}" class="btn btn-outline-success bg-primary">Register</a>
                </div>
            {% endif %}
		</div>
	</div>
</nav>
